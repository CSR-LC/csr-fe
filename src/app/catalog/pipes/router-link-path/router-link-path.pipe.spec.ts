import { RouterLinkPathPipe } from './router-link-path.pipe';

describe('RouterLinkPathPipe', () => {
  it('create an instance', () => {
    const pipe = new RouterLinkPathPipe();
    expect(pipe).toBeTruthy();
  });
});
