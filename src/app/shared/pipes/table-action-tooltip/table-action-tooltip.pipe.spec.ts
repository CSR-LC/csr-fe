import { TableActionTooltipPipe } from '@shared/pipes';

xdescribe('TableActionTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new TableActionTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
