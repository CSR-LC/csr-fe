import { PhoneValidatorDirective } from './phone-validator.directive';

xdescribe('PhoneValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
